export const BASE_URL = "http://localhost:3000";

// StudentAuth;

// api / v1 / student / signup(post);
// api / v1 / student / login(post);
// api / v1 / student / logout(post);
// api / v1 / student / protect(get); //next() middleware route

// TeacherAuth;

// api / v1 / teacher / signup(post);
// api / v1 / teacher / login(post);
// api / v1 / teacher / logout(post);
// api / v1 / teacher / protect(get); //next() middlewareroute

// TeacherProfile;

// api / v1 / teacherprofile / create(post);
// api / v1 / teacherprofile / getTeacherProfile / id(get);
//api / v1 /teacherprofile / findteacher / :id (get)

// Student;

// api / v1 / student / getStudent(post);
// api / v1 / student / sendRequest(put); ////protected route
// api / v1 / student / leaveTeacher(put); //protected route

// Teacher;

// api / v1 / teacher / getTeacher(get);
// api / v1 / teacher / acceptRequest(put); // protected route
// api / v1 / teacher / removeStudent(put); //protected route

// Category;

// api / v1 / category / create(post);
// api / v1 / category / getAllCategories(get);
// api / v1 / category / getCategoryById(get);

//api/v1/teacherprofile/getCurrentStudents(get)
